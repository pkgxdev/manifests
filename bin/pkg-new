#!/bin/sh

if [ ! "$1" ]; then
  echo "usage: pkg-new <package-name>"
  exit 1
fi

cd "$(dirname "$0")"/..

mkdir -p projects/$1

cat <<EOF >projects/$1/package.yml
platforms:
  - darwin/aarch64
  - linux/x86-64
EOF

cat <<EOF > projects/$1/build.ts
import { BuildOptions, unarchive, run } from "brewkit";

export default async function ({ prefix, version, deps, tag, props }: BuildOptions) {
  await unarchive(\`https://foo.com/${version}.tgz\`);
  run\`make\`;
}
EOF

cat <<EOF > projects/$1/test.ts
import { run } from "brewkit";

export default function() {
}
EOF

if [ -f $HOME/src/pantry/projects/$1/package.yml ]; then
  code . projects/$1 projects/$1/package.yml $HOME/src/pantry/projects/$1
else
  code . projects/$1 projects/$1/package.yml
fi